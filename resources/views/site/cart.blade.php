@extends('layouts.site') @section('title','Cart — Emerald Rozalia') @section('content')<section class="page-hero"><span class="eyebrow">YOUR BAG</span><h1>Shopping Cart</h1></section><section class="section cart-layout"><div class="cart-list">@forelse($items as $i)<article class="cart-row"><div><strong>{{$i['name']}}</strong><small>{{$i['sku']}} @foreach($i['options'] as $k=>$v) · {{ucfirst($k)}}: {{$v}} @endforeach</small></div><div>€{{number_format($i['price'],2)}}</div><form method="post" action="{{route('cart.update',$i['key'])}}">@csrf @method('PATCH')<input type="number" name="quantity" min="0" max="50" value="{{$i['quantity']}}"><button>Update</button></form><form method="post" action="{{route('cart.remove',$i['key'])}}">@csrf @method('DELETE')<button>Remove</button></form></article>@empty<p>Your cart is empty. <a href="/shop">Browse the shop</a>.</p>@endforelse</div><aside class="summary-card"><h2>Summary</h2><p>Subtotal <strong>€{{number_format($subtotal,2)}}</strong></p><p>Shipping is calculated at checkout.</p>@if($items)<a class="btn" href="{{auth()->check()?'/checkout':'/login'}}">CHECKOUT</a>@endif</aside></section>@endsection
