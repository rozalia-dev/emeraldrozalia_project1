@extends('layouts.site') @section('title','Checkout — Emerald Rozalia') @section('content')<section class="page-hero"><span class="eyebrow">SECURE CHECKOUT</span><h1>Checkout</h1></section><section class="checkout-layout"><form class="checkout-form" method="post" action="{{route('checkout.store')}}">@csrf<h2>Delivery Details</h2><input name="name" value="{{auth()->user()->name}}" required placeholder="Full name"><input name="email" type="email" value="{{auth()->user()->email}}" required><input name="phone" value="{{auth()->user()->phone}}" placeholder="Phone"><input name="line1" required placeholder="Address line 1"><input name="line2" placeholder="Address line 2"><input name="city" required placeholder="City"><input name="county" placeholder="County"><input name="postcode" placeholder="Postcode"><input name="country" value="IE" maxlength="2" required><h2>Shipping</h2><select name="shipping_method"><option value="">Standard / configured rate</option>@foreach($shippingMethods as $m)<option value="{{$m->code}}">{{$m->name}} — €{{number_format($m->price,2)}}</option>@endforeach</select><h2>Payment</h2><label><input type="radio" name="payment_method" value="cod" checked> Pay on delivery</label><label><input type="radio" name="payment_method" value="bank_transfer"> Bank transfer</label><label><input type="radio" name="payment_method" value="manual"> Manual payment / payment gateway hook</label><input name="discount_code" placeholder="Discount code"><textarea name="notes" placeholder="Order notes"></textarea><button class="btn">PLACE ORDER</button></form><aside class="summary-card"><h2>Your Order</h2>@foreach($items as $i)<p>{{$i['quantity']}} × {{$i['name']}} <strong>€{{number_format($i['price']*$i['quantity'],2)}}</strong></p>@endforeach<hr><p>Subtotal <strong>€{{number_format($subtotal,2)}}</strong></p></aside></section>@endsection
