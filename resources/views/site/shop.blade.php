@extends('layouts.site') @section('title','Shop Hats & Caps — Emerald Rozalia') @section('content')<section class="page-hero"><h1>{{isset($activeCategory)?$activeCategory->name:'SHOP ALL HATS & CAPS'}}</h1><p>Irish made. Premium quality. Made in Limerick.</p></section><section class="shop-layout"><aside class="filters"><h3>SHOP CATEGORIES</h3><a href="/shop">All Hats & Caps</a>@foreach($categories as $c)<a href="{{route('category',$c)}}">{{$c->name}}</a>@endforeach</aside><div class="product-grid">@forelse($products as $product)<article class="product-card"><a href="{{route('product',$product)}}"><div class="product-image">@if($product->image)<img src="{{Storage::url($product->image)}}" alt="{{$product->name}}">@else<span>⌒</span>@endif</div><h3>{{$product->name}}</h3><p>€{{number_format($product->price,2)}}</p><div class="product-tools"><span>360° VIEW</span><span>TRY IT ON</span></div></a></article>@empty<div class="empty">Products can be added from the admin cPanel or bulk upload.</div>@endforelse</div></section><div class="pagination">{{$products->links()}}</div>@endsection