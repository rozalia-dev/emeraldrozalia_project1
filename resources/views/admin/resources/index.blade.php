@extends('layouts.admin') @section('title',ucwords(str_replace('-',' ',$module))) @section('content')<div class="admin-title"><div><small>ADMIN / OPERATIONS</small><h1>{{ucwords(str_replace('-',' ',$module))}}</h1></div></div><div class="panel"><h2>Add Record</h2><form class="module-toolbar" method="post" action="{{route('admin.resource.store',$module)}}">@csrf<input name="title" placeholder="Title / name" required><input name="reference" placeholder="Reference"><select name="status"><option>active</option><option>planned</option><option>in-progress</option><option>completed</option><option>on-hold</option></select><input name="amount" type="number" step="0.01" placeholder="Amount"><input name="record_date" type="date"><input name="notes" placeholder="Notes"><button class="btn">ADD</button></form></div><div class="panel"><table class="data-table"><thead><tr><th>Reference</th><th>Title</th><th>Status</th><th>Amount</th><th>Date</th><th>Action</th></tr></thead><tbody>@forelse($records as $r)<tr><td>{{$r->reference}}</td><td>{{$r->title}}</td><td>{{$r->status}}</td><td>{{$r->amount!==null?'€'.number_format($r->amount,2):'—'}}</td><td>{{$r->record_date?->format('d M Y')??'—'}}</td><td><form method="post" action="{{route('admin.resource.destroy',[$module,$r])}}">@csrf @method('DELETE')<button>Delete</button></form></td></tr>@empty<tr><td colspan="6">No records yet. Use the form above to create the first one.</td></tr>@endforelse</tbody></table>{{$records->links()}}</div>@endsection
