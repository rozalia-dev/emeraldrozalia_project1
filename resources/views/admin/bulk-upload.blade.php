@extends('layouts.admin') @section('title','Bulk Product Upload') @section('content')<div class="admin-title"><div><h1>Bulk Product Upload</h1><p>Upload CSV or Excel files, validate and import products.</p></div></div>@if(session('result'))<div class="panel"><b>{{session('result.imported')}} imported</b><p>{{count(session('result.errors'))}} errors</p></div>@endif<div class="stepper"><span class="active">1 Upload File</span><span>2 Map Columns</span><span>3 Validate</span><span>4 Preview</span><span>5 Import</span></div><section class="panel upload-panel"><form method="post" enctype="multipart/form-data" action="{{route('admin.bulk-upload.store')}}">@csrf<div class="dropzone"><h2>Upload CSV / XLSX</h2><input type="file" name="file" accept=".csv,.xlsx,.xls" required><p>Maximum file size 20MB.</p></div><h3>Supported Fields</h3><div class="tags">@foreach(['Product Name','SKU','Category','Price','Stock','Description','Material','Status'] as $f)<span>{{$f}}</span>@endforeach</div><button class="btn">VALIDATE & IMPORT</button></form></section>@endsection