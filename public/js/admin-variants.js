document.addEventListener('DOMContentLoaded',()=>{const page=document.querySelector('[data-variant-page]');if(!page)return;const dialogs=new Map([...document.querySelectorAll('dialog')].map(d=>[d.id,d]));document.querySelectorAll('[data-open-dialog]').forEach(b=>b.addEventListener('click',()=>{const d=dialogs.get(b.dataset.openDialog);if(d&&!b.disabled){if(d.id==='vmBulkDialog')syncBulkInputs();d.showModal();}}));document.querySelectorAll('[data-close-dialog]').forEach(b=>b.addEventListener('click',()=>b.closest('dialog')?.close()));const checks=[...document.querySelectorAll('[data-variant-check]')],all=document.querySelector('[data-check-all]'),count=document.querySelector('[data-selected-count]');const refresh=()=>{const n=checks.filter(c=>c.checked).length;if(count)count.textContent=`${n} selected`;if(all){all.checked=n>0&&n===checks.length;all.indeterminate=n>0&&n<checks.length;}};checks.forEach(c=>c.addEventListener('change',refresh));all?.addEventListener('change',()=>{checks.forEach(c=>c.checked=all.checked);refresh();});refresh();function syncBulkInputs(){const host=document.querySelector('[data-bulk-variant-inputs]');if(!host)return;host.innerHTML='';checks.filter(c=>c.checked).forEach(c=>{const i=document.createElement('input');i.type='hidden';i.name='variants[]';i.value=c.value;host.appendChild(i);});}const action=document.querySelector('[data-bulk-action]'),stock=[...document.querySelectorAll('[data-stock-field]')];const stockFields=()=>stock.forEach(el=>el.hidden=action?.value!=='set_stock');action?.addEventListener('change',stockFields);stockFields();document.querySelector('[data-bulk-action-form]')?.addEventListener('submit',e=>{syncBulkInputs();if(!checks.some(c=>c.checked)){e.preventDefault();alert('Select at least one variant first.');return;}if(action?.value==='delete'&&!confirm('Delete the selected variants? This cannot be undone.'))e.preventDefault();});const editDialog=document.getElementById('vmEditDialog'),editForm=editDialog?.querySelector('[data-edit-form]');document.querySelectorAll('[data-edit-variant]').forEach(b=>b.addEventListener('click',()=>{const data=JSON.parse(b.dataset.editVariant||'{}');if(!editForm||!editDialog)return;editForm.action=editForm.dataset.actionTemplate.replace('__ID__',data.id);editForm.querySelectorAll('[data-field]').forEach(f=>{const v=data[f.dataset.field];if(f.type==='checkbox')f.checked=Boolean(v);else f.value=v??'';});editDialog.showModal();}));document.querySelector('[data-copy-uuid]')?.addEventListener('click',async()=>{const u=document.querySelector('[data-selected-uuid]')?.textContent?.trim();if(!u||u==='—')return;try{await navigator.clipboard.writeText(u);}catch{}});});
